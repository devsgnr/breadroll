---
title: .isNull
description: Returns a new dataframe where some of it properties have a null
---

#### `Dataframe.isNull`

This function return a new Dataframe where some of it properties have a `null`

Returns

- `Dataframe<T>`

```typescript
const isNull: Dataframe<T> = df.isNull;
```
