---
title: .notNull
description: Returns a new dataframe where none of it properties have a null, the inverse of Dataframe.isNull
---

#### `Dataframe.notNull`

This function return a new Dataframe where none of it properties have a `null`, the inverse of `Dataframe.isNull`

Returns

- `Dataframe`

```typescript
const notNull: Dataframe = df.notNull;
```
