---
title: Local
description: Opens the local data source, ie. the file on disk and return a Dataframe
---

#### `Breadroll.open.local<T>(...)`

This function opens the local data source, ie. the file on disk and return a Dataframe

Parameter

- `filepath: string` - the location of the file.

Returns

- `Promise<Dataframe<T>>`

```typescript
const csv: Breadroll = new Breadroll({ header: true, delimiter: "," });
const df: Dataframe<T> = await csv.open.local<T>("./path/to/file.csv");
```
